import './Banner.css'

function Banner() {
    // JSX = como o React le isso e transforma em elemento no DOM
    return (
        <header className="banner">
            <img src="/imagens/banneratt.png" alt="banner principal da pagina do organo"/>
        </header>
    )
}

export default Banner